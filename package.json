{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "pnpm -r build --filter=!extension",
    "dev": "pnpm -r dev --filter=!extension",
    "lint": "pnpm -r lint --filter=!extension",
    "prettier": "prettier --write packages/*",
    "eslint:fix": "eslint --fix packages/*"
  },
  "devDependencies": {
    "@vercel/style-guide": "^6.0.0",
    "eslint": "^8.57.1",
    "prettier": "^3.3.3"
  }
}
